package tcpframework;

/**
 * An enumeration representing the HTTP methods supported by the framework.
 * These methods correspond to the standard HTTP request methods.
 */
public enum HTTPMethode {
    GET,
    POST,
    PUT,
    DELETE,
    HEAD,
    OPTIONS,
}
